/**
 * RTE Test — body field uses rtetest filter (toolbar/actions in _rtetest.json).
 * @param {Element} block The block element
 */
export default function decorate(block) {
  block.classList.add('rtetest');
}
